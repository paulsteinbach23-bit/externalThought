// Copy this file to config.js and fill in your Supabase project's values.
// The anon key is meant to be public in client-side apps — RLS (see
// supabase_schema_v3_auth_lockdown.sql) is what actually protects data, not
// hiding this file. config.js is committed for real in this repo; this
// template is only here for anyone spinning up a separate Supabase project.
// Project → Settings → API in the Supabase dashboard.

const SUPABASE_URL = 'https://YOUR_PROJECT.supabase.co';
const SUPABASE_ANON_KEY = 'YOUR_ANON_KEY';
